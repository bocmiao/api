# 更新日志

在线更新会读取这个文件，把比当前版本更新的内容展示在管理后台。每次发布新版本时：
1. 修改 `package.json` 中的 `version`；
2. 在下面最上方加一段 `## v版本号 · 日期`，每条改动一行，以 `- ` 开头。

## v0.9.0 · 2026-09-26
- 网址不再带 `/#/`：接口详情改为 `/docs/接口名`（如 `/docs/epic`），控制台、管理后台、运行状态等页面也都是普通地址，可以直接收藏和分享
- 旧的 `#/` 链接会自动跳转到新地址，之前收藏或分享出去的链接继续可用
- 站内切换页面仍然不整页刷新，浏览器的前进、后退正常可用
- 以 `/api/` 开头的接口地址和 `/s/` 短链接不变

## v0.8.9 · 2026-09-26
- 管理后台新增「一键巡检」：用每个接口的示例参数依次实际调用一次，实时显示进度，列出失败、超时的接口，并可直接点「AI 分析」查原因
- 巡检不计入调用额度、不算进调用统计；默认跳过多节点检测、AI、整站抓取、批量检测、短链接，勾选后也可一起巡检
- 示例参数不适用的接口单独标为「参数不适用」，不与真正的故障混在一起

## v0.8.8 · 2026-09-26
- 上传更新包更安全：上传前需要再输入一次管理员密码，换取 5 分钟内有效、只能用一次的确认码，登录状态被盗也无法直接上传
- 上传的更新包会逐个文件与 GitHub 官方代码比对，必须是本仓库分支上的原始代码，任何文件被改动都会拒绝（可在「系统设置 → 在线更新」关闭，不建议）

## v0.8.7 · 2026-09-26
- 管理后台「系统更新」新增「上传更新包」：服务器下载 GitHub 太慢时，在电脑上下载代码包（zip）后直接在网页上传，服务器同样会检查、备份、替换并自动重启，失败自动恢复
- 上传时显示上传进度，最大 60 MB，支持 GitHub 的 Download ZIP 和 .tar.gz

## v0.8.6 · 2026-09-26
- IP 归属地改为优先查本地离线库（ip2region）：国内 IP 精确到城市和运营商，几乎不耗时、没有次数限制；境外 IP 和 IPv6 仍查在线接口补全
- 手机号归属地改为优先查本地号段库，新增区号和邮编；本地查不到时再查在线接口
- 修复首页天气按访客 IP 定位一直没有生效的问题

## v0.8.5 · 2026-09-26
- 新增天气城市搜索 /api/weather/city：输入城市或区县名，列出匹配的地点及编号、省市、经纬度，方便区分重名地名
- 天气接口支持按城市编号（id）查询；按城市名查询时额外返回其他同名候选（candidates）

## v0.8.4 · 2026-09-26
- 首页每个接口卡片显示运行状态（正常 / 不稳定 / 异常）、累计调用次数和今日调用次数
- 新增累计调用统计，不会随调用日志定期清理而减少

## v0.8.3 · 2026-09-26
- 管理后台「接口开关」标注每个接口是否需要第三方密钥：「需填 Key」不填无法使用，「可选 Key」不填也能用、填了更好，已配置的显示「已填 Key」
- 点标签直接跳到系统设置里对应的密钥并高亮；顶部显示还有几个接口待填 Key

## v0.8.2 · 2026-09-26
- 顶部导航只保留一个「管理」入口，概览、用户、更新记录、系统设置统一在管理页右上角切换，不再重复

## v0.8.1 · 2026-09-26
- 导航栏切换页面不再每次重新加载：浏览器记住各页面上次的数据，点开立即显示，同时在后台刷新
- 登录后空闲时预先取好管理、用户、更新记录、设置、控制台等页面的数据，第一次点进去也不用等
- 修复多次进入「设置」页后，按钮会重复触发的问题

## v0.8.0 · 2026-09-26
- 新增「AI 分析」：管理后台的接口调用排行和服务端错误列表里，每个接口都可以一键让 AI 分析失败原因，给出结论、可能原因和处理建议
- AI 分析会汇总该接口最近 7 天的调用和失败原因、所需密钥是否已配置，并实际调用一次，再交给「设置 → AI」里配置的大模型判断
- 调用日志开始记录失败原因（只记错误说明，不记请求参数），方便排查

## v0.7.4 · 2026-09-26
- 修复国内服务器增量更新总是失败：以前只从 raw.githubusercontent.com 下载（国内经常连不上），现在依次尝试加速地址、jsDelivr、raw.githubusercontent.com、GitHub 接口，连不上的来源本次不再重试
- 进度条显示文件是从哪个来源下载的；增量更新失败时把原因写入运行日志
- 检查更新时 raw.githubusercontent.com 连不上会在 5 秒内放弃，并在 30 分钟内直接走 GitHub 接口，不再每次卡住

## v0.7.3 · 2026-09-26
- 管理后台「系统更新」直接显示当前版本和最近一次在线更新的时间，不用先点「检查更新」
- 顶部导航新增「更新记录」（仅管理员可见），列出每个版本改了什么，并标出当前版本
- 用户列表默认每页 20 个，可选 10 / 20 / 50 / 100

## v0.7.2 · 2026-09-26
- 接口调用排行改为每页 10 个，可上一页 / 下一页翻看全部接口
- 用户列表改为每页 10 个

## v0.7.1 · 2026-09-26
- 用户管理独立成页：顶部导航新增「用户」（仅管理员可见），支持按邮箱搜索、分页，并显示最近调用时间和近 7 天调用次数
- 管理后台「热门接口」改为「接口调用排行」，统计全部接口：默认显示前 15 个，可点「展开全部」查看其余

## v0.7.0 · 2026-09-26
- 在线更新改为增量更新：只下载有改动的文件，没变的文件直接复用，平时一次更新只需下载几十 KB，不用再下载整个 5 MB 多的更新包
- 每个下载的文件都会核对 GitHub 官方指纹，走加速地址也不会被篡改；改动太多或出错时自动改为下载完整更新包
- 进度条显示「已下载 x / y 个改动文件，z 个文件没有变化」

## v0.6.10 · 2026-09-26
- 系统更新区分「正在运行的版本」和「服务器上的代码版本」：代码替换后还没重启服务时，醒目提示需要重启
- 手动上传代码后不再误报「有新的修复可以更新」

## v0.6.9 · 2026-09-26
- 检查更新大幅减少 GitHub 接口调用：版本号和更新日志改从 raw.githubusercontent.com 读取，检查结果缓存 1 分钟，「立即更新」复用刚才的检查结果
- GitHub 接口次数用完时提示多久后恢复，并说明填写 GitHub Token 后每小时 5000 次
- 填写了 GitHub Token 时也可以使用下载加速地址

## v0.6.8 · 2026-09-26
- 在线更新支持「GitHub 下载加速地址」（系统设置 → 在线更新），解决国内服务器下载 GitHub 慢、更新超时失败的问题
- 通过加速地址下载的每个文件都会与 GitHub 官方记录逐一核对，发现被改动立即拒绝；加速失败自动改为直连
- 更新包下载超时由 2 分钟放宽到 5 分钟

## v0.6.7 · 2026-09-25
- 在线更新显示实时进度条：检查、下载（已下载 / 总大小）、解压、试启动、替换、重启，每一步都能看到
- 更新改为在后台执行，不会再因为请求时间太长被 CDN / 反向代理断开（显示 HTTP 554 等错误）
- 重启完成后自动确认新版本号并刷新页面

## v0.6.6 · 2026-09-25
- 接口、登录、短链跳转等响应默认带 Cache-Control: no-store，即使 CDN 规则没配好也不会被边缘节点缓存
- 二维码、徽章、头像等图片接口的缓存改为只允许浏览器缓存（private），不被 CDN 缓存，调用次数照常统计

## v0.6.5 · 2026-09-25
- 修复首页标题的接口数量与下方「全部接口」不一致：标题按接口个数统计，另外单独显示调用地址总数

## v0.6.4 · 2026-09-25
- 接口提速：缓存刚过期时先返回上一份数据，同时在后台刷新，调用方不再等待上游
- 缓存预热：每 3 分钟在后台刷新最近 24 小时最常用的接口，服务重启或在线更新后也会自动预热
- 页面脚本和样式带内容指纹的地址改为长期缓存，配合 CDN（如腾讯云 EdgeOne）可直接在边缘节点返回
- 开启「信任反向代理」后支持腾讯云 EdgeOne 的 EO-Connecting-IP 请求头，正确识别访客真实 IP

## v0.6.3 · 2026-09-25
- 首页「今日」秒开：浏览器记住上次的数据先显示，再在后台换成最新数据
- 服务器启动时预先准备好「今日」数据并每 5 分钟自动刷新，访客不再需要等待上游接口
- 天气单独加载，慢了也不影响其他卡片

## v0.6.2 · 2026-09-25
- 首页「今日」天气改为按访客所在城市显示（按 IP 定位），也可点「切换城市」手动指定，例如区县
- 首页「今日」卡片重新排版：新增「历史上的今天」卡片补齐空位（取不到时显示国际金价），微博热搜显示 10 条

## v0.6.1 · 2026-09-25
- 系统设置里的第三方服务（快递100、DeepL、百度翻译、有道、和风天气、IsThereAnyDeal、CoinGecko、Globalping、大模型、GitHub）新增「测试连通性」按钮，直接显示密钥是否有效、剩余额度等
- 每个密钥下方新增「获取地址」链接，点击直达对应平台的密钥申请页面

## v0.6.0 · 2026-09-25
- 新增多节点检测（Globalping 全球探针）：多节点 Ping / TCP Ping、HTTP 测速、DNS 查询、路由追踪，按国家和城市汇总延迟与丢包；可在「系统设置 → 第三方密钥」填写 Globalping Token 提高额度
- 新增 DNS 传播检测：同时查询 Google、Cloudflare、阿里、腾讯等 7 个公共 DNS，对比解析是否一致
- 新增批量检测：批量 Ping、批量 TCPing、批量 HTTP，一次最多 10 个目标（按目标数计入调用次数）
- 新增 IP 信誉查询：公共黑名单、机房 IP 识别、反向解析，给出风险评分

## v0.5.0 · 2026-09-25
- 成语词库扩充到约 3 万条（新华词典），成语接龙默认使用全部词库；歇后语可选用 1.2 万条扩充词库（已过滤歧视性内容）
- 新增汉字字典（约 1.4 万字）、古诗词检索与随机（唐诗三百首、宋词三百首）
- 新增整站文字抓取：沿站内链接批量提取文字，遵守 robots.txt 和抓取间隔，页面上限可在「系统设置 → 其他」调整
- 新增站长工具：HTTP 安全头检测、邮件安全检测（SPF / DMARC / DKIM）、邮箱有效性检测、RSS 转 JSON、网页正文提取、网页转 Markdown、苹果应用搜索
- 新增生活计算：个税计算、身份证校验、经纬度距离、单位换算（含市制）、年龄生肖星座、工作日计算、健康指标、数独
- 新增开发工具：子网计算、IP 与整数互转、进制转换、Unicode 转换、经典密码、JWT 解码验签、TOTP 两步验证码、密码强度、请求回显、测试数据、文本统计、人民币大写、JSON 格式化、Cron 解析、正则测试、占位图、徽章、文字转图片

## v0.4.0 · 2026-09-25
- 新增「AI」分类：文本摘要、情感分析、AI 翻译、AI 对话（需登录，在「系统设置 → AI」填写密钥，兼容 DeepSeek 等 OpenAI 格式服务）
- 新增垃圾分类查询（330 种物品，按国标分类，支持模糊匹配）
- 新增成语词典与成语接龙、歇后语、脑筋急转弯（可隐藏答案）、冷知识、彩虹屁、笑话
- 新增 Minecraft 服务器状态查询（在线人数、版本、延迟，支持 SRV 记录）
- 首页新增「今日」卡片：日期节气、Epic 周免、热榜速览
- 新增「运行状态」页面，显示各接口分类是否可用
- 按 Ctrl+K 可快速搜索接口
- 在线调试的返回结果可显示字段中文注释
- 开发文档新增「请求方式：GET 和 POST」，附 HTML、fetch、jQuery、Python、PHP、curl 示例

## v0.3.3 · 2026-09-25
- 开发文档新增「在网站 / 博客主题里调用」：说明免 Key 调用、跨域、额度，并以 Epic 周免为例
- 每个接口的示例代码新增「网页前端」写法（不带 Key），图片类接口给出 <img> 标签用法

## v0.3.2 · 2026-09-25
- 修复腾讯企业邮等服务报「mail from address must be same as authorization user」无法发信的问题：发信地址始终使用邮箱账号，「发件人名称」只作为显示名

## v0.3.1 · 2026-09-25
- 修复在线更新后浏览器仍显示旧页面、需要强制刷新的问题
- 管理员顶部导航新增「设置」入口，邮件、密钥等配置更好找
- 系统更新改为显示版本号和中文更新说明

## v0.3.0 · 2026-09-25
- 项目更名为 Miao API，换上猫咪图标
- 每个接口的返回字段都有中文说明（类型、单位、格式、何时为空）
- 新增 7 个娱乐接口：答案之书、今日运势、号码吉凶、语录合集、温馨提示、随机头像等
- 新增 7 个工具接口：摸鱼日历、BMI、图形验证码、访客信息、UA 解析、颜色转换、IP 签名档
- 新增「网络」分类 10 个接口：DNS、SSL 证书、网站检测、TCPing、Ping、域名注册查询等
- 注册需要邮箱验证码，支持邮箱找回密码，并有图形验证码和发送频率限制防刷
- 管理后台新增「系统设置」：邮件、额度、第三方密钥等都能在网页上配置，保存即生效
- 管理后台新增「接口开关」：可按接口关闭，关闭后所有人都无法调用
- 管理后台新增「系统更新」：一键从 GitHub 更新，失败自动回滚
- 修复约 30 处数据解析与安全问题

## v0.2.0 · 2026-09-24
- 聚合 46 个接口：游戏限免、全网热榜、天气节假日、金融行情、壁纸诗词、开发工具
- 账号注册登录、API Key、按 IP / 账号的每日额度与限流
- 调用统计、管理后台
- 订阅推送：Server酱、Bark、Telegram、钉钉、飞书、企业微信、Webhook、邮件
- 全新网页界面，支持深色模式和手机访问

## v0.1.0 · 2026-09-24
- 首个版本：Epic 每周免费游戏接口
